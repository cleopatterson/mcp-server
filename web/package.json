{
  "name": "painting-carousel",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/PainterCarousel.jsx --bundle --format=esm --outfile=dist/component.js --jsx=automatic --external:react --external:react-dom"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
